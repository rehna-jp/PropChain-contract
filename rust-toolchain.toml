# Rust configuration
# This file contains configuration for the Rust toolchain and workspace

[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
targets = ["wasm32-unknown-unknown"]
profile = "default"
